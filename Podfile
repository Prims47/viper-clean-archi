# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'BusinessLogic' do
  # Comment the next line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!

  # Pods for BusinessLogic
  pod 'SwiftLint'
  pod 'Sourcery'

  target 'BusinessLogicTests' do
    inherit! :search_paths
    # Pods for testing
    pod 'Nimble'

  end

end

target 'DataProxy' do
  # Comment the next line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!

  # Pods for DataProxy
  pod 'SwiftLint'

  target 'DataProxyTests' do
    inherit! :search_paths
    # Pods for testing
    pod 'Nimble'

  end

end

target 'viperCleanArchi' do
  # Comment the next line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!

  # Pods for viperCleanArchi
  pod 'SwiftLint'
  pod 'SwiftGen'

  target 'viperCleanArchiTests' do
    inherit! :search_paths
    # Pods for testing
    pod 'Nimble'

  end

end
